# Migration `20260807204918_returns_management` — لا تحذف هذا المجلد

> **تحذير إلزامي (AGENT.md §3.2):** هذا المجلد **مُطبَّق فعليًا** على قاعدة البيانات ومسجّل في جدول `_prisma_migrations`. حذفه أو تعديله سيؤدي إلى كسر تطابق الـ schema بين البيئات.

## ما هذا؟

- Migration **فارغة** (`migration.sql` بحجم 0 بايت).
- سُمِّيت `returns_management` وقبلت تسلسل المرحلة، ثم **بُدئ تنفيذها على DB وتم تسجيلها في `_prisma_migrations`** قبل أن يُلغى محتواها فعليًا (تُرك الاسم للحفاظ على ترتيب المراحل وعدم إعادة إنشاء migration باسم مختلف قد يتعارض مع البيئات).
- المحتوى الحقيقي لبنية المرتجعات موجود في الـ migration التالية: **`20260807204935_returns_management`**.

## ليش مش بنحذفها؟

1. حذف migration مُطبَّقة على أي بيئة = انتهاك صريح لـ AGENT.md §3.2.
2. الـ Prisma يعتمد على تسلسل الـ migrations وعلامة `applied` في `_prisma_migrations`؛ حذفها قد يسبب حالة غير متسقة عند `migrate deploy` أو `migrate status`.
3. وجود migration فارغة باسمها الحالي لا يسبب أي ضرر وظيفي (لا SQL يُنفَّذ)، لكنه يحافظ على استقرار سجل الهجرات.

## القرار النهائي

- **ممنوع** حذف المجلد `20260807204918_returns_management`.
- **ممنوع** تعديل `migration.sql` الخاص به لاحقًا (لأنه مسجّل كمُطبَّق).
- أي تغيير جديد في الـ schema = migration جديدة بالاسم التالي في التسلسل.

## ملفات التصميم المرجعية

- `docs/design/returns-management-design.md`
- `docs/permissions.md` (قسم `returns.*`)
