{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules && npm install && npx prisma generate && npx tsc"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx prisma db push --skip-generate --accept-data-loss && node dist/seed-import.js && node dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
